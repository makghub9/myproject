server:
  port: 9501
management:
  security:
    enabled: false
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/makghub9/myconfigrepo
          search-paths: myconfigrepo
  application:
    name: cloudconfigserver